# About

Create the graphql schema for an organisation.

# Effect

The `OrganisationProvider` service from `@pf/process` is used to expose
the current organisation to any service which needs this access.
